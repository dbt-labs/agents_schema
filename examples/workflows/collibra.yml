name: Agents Schema Collibra

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  agents-schema-collibra:
    uses: fivetran/agents_schema/.github/workflows/agents-schema-collibra.yml@v0.0.6
    with:
      metadata-path: metadata/collibra-assets.json
    secrets: inherit
