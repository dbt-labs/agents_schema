name: Agents Schema Atlan

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  agents-schema-atlan:
    uses: fivetran/agents_schema/.github/workflows/agents-schema-atlan.yml@v0.0.6
    with:
      metadata-path: metadata/atlan-assets.json
    secrets: inherit
